const MONEY_CONSTANTS = Object.freeze({
  minAmount: 1000,
  countUnit: 1000,
});

const LOTTO_CONSTANTS = Object.freeze({
  minRange: 1,
  maxRange: 45,
  digits: 6,
});

module.exports = { MONEY_CONSTANTS, LOTTO_CONSTANTS };
