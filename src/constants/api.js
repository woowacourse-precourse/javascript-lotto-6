export const PURCHASE_AMOUNT = Object.freeze({
  MAX: 100000,
});

export const LOTTO = Object.freeze({
  PRICE: 1000,
  NUMBER_CHOICES_PER_TICKET: 6,
  MIN_NUMBER_IN_RANGE: 1,
  MAX_NUMBER_IN_RANGE: 45,
})