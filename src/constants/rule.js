export const REGEX = {
  ONLY_NUMBER: /^[0-9]+$/,
  ONLY_NUMBER_AND_COMMA: /^[0-9,]+$/,
};

export const UNIT = {
  PURCHASE: 1000,
};

export const LOTTO = {
  MIN_RANGE: 1,
  MAX_RANGE: 45,
  LENGTH: 6,
};

export const SEPARATOR = {
  PURCHASED_LOTTO: ", ",
  USER_LOTTO: ",",
  NAME: "쉼표",
};
