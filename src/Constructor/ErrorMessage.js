import { LOTTO_NUMBER_COUNT } from "./Setting";

const ERROR_PREFIX = "[ERROR]";
const ERROR_MESSAGE = {
  INVALID_LOTTO_NUMBER_COUNT: `${ERROR_PREFIX} 로또 번호는 ${LOTTO_NUMBER_COUNT}개여야 합니다.`,
};

export default ERROR_MESSAGE;
