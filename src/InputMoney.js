'use stirct';

class inputMoney {
    constructor(inputMoney){
        this.inputMoney = inputMoney;
        inputMoney.isValid();
    }
    const inputMoney = Number(Console.readLineAsync(MESSAGES.PUT_MONEY));
}