const Converter = {
  /**
   * @param {string} input
   * @param {string} delimiter
   * @returns {string[]}
   */
  split(input, delimiter) {
    return input.split(delimiter);
  },
};

export default Converter;
