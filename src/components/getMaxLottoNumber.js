import { MissionUtils } from '@woowacourse/mission-utils';
import money from './getNumber.js';

export default async function getMaxLottoNumber(money) {
    let count = money / 1000;
    console.log(count);
}

console.log(money);

getMaxLottoNumber(money);
